<div class="container">
    <div class="row">
        <div class="col-sm-12 col-md-6">
            <ul id="voyants">
                {{#each dashBoard.getters}}
                    <li>{{this}}</li>
                {{/each}}
            </ul>
        </div>
        <div class="col-sm-12 col-md-6">

            <div class="btn-group" role="group" aria-label="...">
                {{#each dashBoard.setters}}
                    <button type="button" class="btn btn-default btn-raised">{{this}}</button>
                {{/each}}
            </div>
        </div>
    </div>

    <!-- <div class="row">
        <div class="col-md-12">


            <div class="switch">
                <input id="cmn-toggle-4" class="cmn-toggle cmn-toggle-round-flat" type="checkbox">
                <label for="cmn-toggle-4"></label>
            </div>
    </div> -->
</div>
